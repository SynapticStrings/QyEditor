defmodule DiffSinger.Model do
  @moduledoc """
  模型的通用文档，如果想查看各个模型的实现细节，请参见：

  - `DiffSinger.Model.Acoustic`
  - `DiffSinger.Model.Variance`
  - `DiffSinger.Model.Vocoder`
  - [TODO)

  当前 DiffSinger Elixir 只（还没）支持并且只打算支持
  ONNX 版本（也就是 OpenUTAU 用的）。
  """
end
