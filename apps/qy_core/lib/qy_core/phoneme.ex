defmodule QyCore.Phoneme do
  @moduledoc """
  负责音素相关的业务逻辑。

  其主要负责两个部分：

  * 该语言的正字法 --> 音素
  * 音素的名字 --> id
  """
end

defmodule QyCore.Phoneme.Backend do
  # 音素适配器所适配的 protocol
  # @type phoneme_databaze :: map() | module()
end
